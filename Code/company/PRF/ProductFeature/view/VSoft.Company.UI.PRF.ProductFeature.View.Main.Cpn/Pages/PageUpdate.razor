@using VSoft.Company.UI.PRF.ProductFeature.View.Main.Cpn.Models;
@*@using VSoft.Company.UI.USR.User.View.Share.Components*@
<h3>Sửa thông tin khách hàng</h3>

<div class="form-group">
	<div class="form-group pt-3">
		<label for="name">Tên</label>
		<input id="name" class="form-control" placeholder="Nhập tên khách hàng...."
			@bind-value="@ProductFeature.Name"></input>
	</div>
	
	@*<div class="form-group pt-3">
		<label for="gender">Giới tính</label>
		<InputRadioGroup id="gender" @bind-Value="@ProductFeature.Gender">
			<div>
				<InputRadio id="male" Value="true" checked="checked" />
				<label for="male">Nam</label>
			</div>

			<div>
				<InputRadio id="female" Value="false" />
				<label for="female">Nữ</label>
			</div>
		</InputRadioGroup>

	</div>*@

	@*<div class="form-group pt-3">
		<label for="birthdate">Ngày sinh</label>
		<InputDate id="birthdate" @bind-Value="@ProductFeature.BirthDate" />
	</div>


	<div class="form-group pt-3">
		<label for="priority">Ưu tiên</label>
		<InputSelect id="priority" class="form-control" @bind-Value="ProductFeature.PriorityId">
			<option>Select a priority</option>
			@foreach (var item in priorities)
			{
				<option value="@item.Id">@item.Name</option>
			}
		</InputSelect>

	</div>*@

	@*<div class="form-group pt-3">
	<label for="Name">User</label>
	<SelectUserComponent @bind-SelectedId="@ProductFeature.UserId" />
	</div>*@
	<div class="form-group float-end pt-3">
		<button type="button" @onclick="@OnSubmit" class="btn btn-primary pt-1">Cập nhật</button>
	</div>
</div>

@code {
	static List<Priority> priorities = new List<Priority>
{
new Priority { Id=1, Name="Cao" },
new Priority { Id=2, Name="Trung bình" },
new Priority { Id=3, Name="Thấp" }
};
}